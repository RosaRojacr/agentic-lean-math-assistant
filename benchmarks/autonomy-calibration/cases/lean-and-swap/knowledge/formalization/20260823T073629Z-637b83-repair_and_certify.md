Implemented and certified the conjunction-symmetry proof.

### Source

`proof/Calibration.lean` now contains:

```lean
theorem and_swap_calibration (P Q : Prop) : P ∧ Q → Q ∧ P := by
  intro h
  cases h with
  | intro hP hQ =>
      exact And.intro hQ hP

#print axioms and_swap_calibration
```

Only `exact ?_` was replaced. The theorem declaration and axiom-report command remain unchanged.

### Verification

From `proof/`:

- Prohibited-construct scan: exit `0`, empty stdout/stderr.
- `/home/rosa/.elan/bin/lake build`: exit `0`.
- `/home/rosa/.elan/bin/lake env lean Calibration.lean`: exit `0`.
- Both Lean runs report:

```text
'and_swap_calibration' does not depend on any axioms
```

Exact commands, stdout, stderr, and statuses are retained in:

- `agents/formalization/repair_and_certify/build-evidence.md`
- `agents/formalization/repair_and_certify/evidence/`
