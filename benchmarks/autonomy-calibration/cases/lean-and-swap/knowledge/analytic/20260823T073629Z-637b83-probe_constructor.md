## Current retained source

`proof/Calibration.lean:1-4`:

```lean
theorem and_swap_calibration (P Q : Prop) : P ∧ Q → Q ∧ P := by
  exact ?_

#print axioms and_swap_calibration
```

- Exact declaration: `and_swap_calibration (P Q : Prop) : P ∧ Q → Q ∧ P`
- Unresolved body: `proof/Calibration.lean:2`, `exact ?_`
- The retained statement exactly matches the conjunction-symmetry contract.

Pinned project identity:

- `proof/lean-toolchain`: `leanprover/lean4:v4.33.1`
- Lake package: `lean_and_swap_calibration`, version `0.1.0`
- Default target/library: `Calibration`
- Manifest version: `1.2.0`
- Dependencies: none

## Isolated constructor probe

Created `agents/analytic/probe_constructor/Probe.lean`:

```lean
theorem and_swap_calibration_constructor_probe (P Q : Prop) : P ∧ Q → Q ∧ P := by
  intro h
  cases h with
  | intro hP hQ =>
      exact And.intro hQ hP

#print axioms and_swap_calibration_constructor_probe
```

Exact command, executed from `proof/`:

```text
/home/rosa/.elan/bin/lake env lean ../agents/analytic/probe_constructor/Probe.lean
```

Exit status:

```text
0
```

Exact stdout:

```text
'and_swap_calibration_constructor_probe' does not depend on any axioms
```

Exact stderr: empty.

The current pinned environment accepts the explicit `intro` / `cases` / `And.intro hQ hP` proof, and the isolated probe theorem has an axiom-free report.

Evidence retained at:

- `agents/analytic/probe_constructor/Probe.lean`
- `agents/analytic/probe_constructor/evidence.json`

`evidence.json` was validated with `jq -e .`. Nothing under `proof/` was modified. Per stage restrictions, no project build was invoked and this is not project-build certification.
