Completed and verified the self-contained certificate.

**Retained locator:**  
`agents/certificate/produce_certificate/certificate.md`

It contains:

- The definition of \(S_m\).
- The shifted product \(rS_m\).
- Endpoint-preserving subtraction and cancellation.
- The required condition \(r\ne1\) before division.
- Exact specialization \(m=8\), \(r=2\), including \(2^8=256\) and \(3\cdot255=765\).
- An independent termwise check listing all eight terms and cumulative integer sums ending at \(765\).

**Evidence:** Re-read the retained file after writing; all required components are present and both exact routes conclude \(765\).

**Commands:** No shell commands were used; workspace reads and the artifact write used the harness file tools.

**Failures, limitations, unresolved obligations:** None.
