# Permitted source record

For any number `r` and positive integer `m`, multiplication and subtraction may be used to derive a closed form for `1 + r + ... + r^(m-1)`. No closed form is supplied; it must be derived in the campaign.
