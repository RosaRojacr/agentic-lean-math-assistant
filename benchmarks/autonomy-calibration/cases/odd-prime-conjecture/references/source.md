# Definitions

An integer `p > 1` is prime exactly when its only positive divisors are `1` and `p`. An integer is odd exactly when it is not divisible by `2`. The theorem must cover every integer satisfying its stated hypotheses.
