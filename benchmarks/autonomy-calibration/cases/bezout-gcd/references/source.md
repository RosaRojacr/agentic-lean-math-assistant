# Permitted source record

Euclidean division may be iterated: for integers `a > b > 0`, write `a = qb + r` with `0 <= r < b`. The last nonzero remainder is a greatest common divisor. Back-substitution through the same equalities yields a Bézout linear combination.
