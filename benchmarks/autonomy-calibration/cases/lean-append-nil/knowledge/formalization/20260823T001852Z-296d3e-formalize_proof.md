Implemented the structural-induction proof in `proof/Calibration.lean` without changing the theorem statement or axiom-report command.

```lean
theorem append_nil_calibration {α : Type} (xs : List α) : xs ++ [] = xs := by
  induction xs with
  | nil => rfl
  | cons x xs ih =>
    change x :: (xs ++ []) = x :: xs
    exact congrArg (List.cons x) ih

#print axioms append_nil_calibration
```

Verification from `proof/` using the retained `leanprover/lean4:v4.33.1` toolchain:

```text
$ lake build
ℹ [2/3] Built Calibration (181ms)
info: Calibration.lean:8:0: 'append_nil_calibration' does not depend on any axioms
Build completed successfully (3 jobs).
```

Exit status: `0`.

Retained evidence:

- `agents/formalization/formalize_proof/lake-build.log`
- `agents/formalization/formalize_proof/build-evidence.md`
- `agents/formalization/formalize_proof/lake-build-path-failure.log` — initial exit `127` because the inherited `PATH` omitted `/home/rosa/.elan/bin`; no source repair was required.
