Pilot replay complete without modifying `proof/Calibration.lean`.

- Confirmed current declaration at `proof/Calibration.lean:1` exactly retains:
  ```lean
  theorem append_nil_calibration {α : Type} (xs : List α) : xs ++ [] = xs := by
  ```
- Created disposable candidate:
  `agents/formal_probe/pilot_replay/append_nil_candidate.lean`
- Used the retained structural-induction proof with no imports, placeholders, or additional axioms.
- Verified with pinned Lean 4.33.1:
  ```text
  $ timeout 30s /home/rosa/.elan/bin/lake env lean ../agents/formal_probe/pilot_replay/append_nil_candidate.lean
  'append_nil_calibration' does not depend on any axioms
  ```
  Exit status: `0`. No compiler errors or warnings.
- Recorded commands, statuses, diagnostics, toolchain version, candidate source, and axiom output in:
  `agents/formal_probe/pilot_replay/replay-evidence.md`

The initial invocation through bare `lake` exited `127` because Lake was absent from inherited `PATH`; the evidence records this and the successful absolute-path invocation.
