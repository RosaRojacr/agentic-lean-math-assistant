# Permitted source record

Among any two consecutive integers, one is even. Among any three consecutive integers, one is divisible by `3`. The integers `2` and `3` are coprime. Standard integer factorization and divisibility rules may be used.
