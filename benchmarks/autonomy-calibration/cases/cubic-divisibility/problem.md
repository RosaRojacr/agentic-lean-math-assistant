# Cubic divisibility over the integers

Prove that `6` divides `n³ - n` for every integer `n`.

The proof must cover negative integers and zero, justify both the factorization and every divisibility step, and explain why simultaneous divisibility by `2` and `3` implies divisibility by `6`. Include an independent residue-class check modulo `6`.
