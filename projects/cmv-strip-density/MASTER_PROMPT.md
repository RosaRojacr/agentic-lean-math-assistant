# Master Prompt

## Primary objective

Solve CMV Conjecture 3.12 over its entire admissible range, using Agentic Lean
Math Assistant to produce an honest, reproducible, kernel-checked Lean proof or
a rigorous counterexample. Pursue reduction of the currently unsolved CMV range
as the primary mathematical focus. Improve Agentic Lean Math Assistant whenever
a concrete program defect or workflow bottleneck is obstructing that objective.

The modeled regular type-(iv) exclusion is kernel-checked for every
`lambda > 1`: a universal interior type-(iv) stationary root, finite
half-height equal-area bracket, and checked variational transfer construct an
actual source-admissible type-(iii) competitor with strictly smaller relaxed
source perimeter for every canonical profile. The immediate proof-discovery
milestone is universal source classification, including endpoint handling,
into the checked exact- or almost-everywhere-horizontal carrier interface.
Arbitrary-competitor model coverage remains an explicit unresolved auxiliary
route, not a premise of the direct comparison.

## Current evidence

- Every modeled type-(iv) candidate satisfying the CMV scalar hypotheses is
  kernel-proved not to minimize weighted perimeter for every `lambda > 1`.
- A denominator-free fold IVT, finite endpoint area bracket, and type-(iii)
  area IVT construct a strictly ordered stationary equal-area pair. The
  type-(iii) fold, perimeter difference, and reduced fold gap are all strictly
  negative at that pair.
- Canonical, exactly horizontally congruent, and almost-everywhere horizontally
  congruent regular source carriers are directly excluded without
  `NormalizationWitness`, `CompatibleWithModel`, or `model_covered` supplied by
  the caller. Arbitrary unclassified source carriers remain outside the checked
  theorem.
- The actual `TypeThreeAssembly` carrier has kernel-checked weighted area,
  complete-frontier weighted perimeter, and finite-perimeter competitor
  realization.
- The corrected near-one fold, cosine, and area equations have an exact
  pole-free `s^2` regularization, global rational remainder bounds, and the exact
  cusp seed `(pi, 5*pi/12, -44 + 19*pi^2/24)` at `s = 0`.
- The actual corrected root map has a kernel-checked cusp Jacobian with
  determinant `-768*pi < 0`, an exact unique branch tangent, and a
  tangent-centered shear chart with Jacobian `diag(2, 96, 4*pi)`.
- The complete quadratically rescaled oriented root map is kernel-proved
  continuous at `s = 0`; its third row uses a staged exact two-jet proof whose
  forced build now completes in 33 seconds.
- A Fin-3 Poincare-Miranda proof gives an exact positive-`s` source root.
- All three endpoint-box face pairs, and therefore all six composed face signs,
  are independently certified for `0 <= s <= 1/126334`. On the same radius,
  the pole-free reduced gap differs from its cusp value by at most `59916*s`
  and is strictly below `-19/2`.
- The four centered arctangent square remainders are at most
  `1/8000000000` on the full common radius. The analytic regularized third row
  differs from its frozen rational row by at most `s^2/1000`, permitting the
  compatible frozen-row allowance `3999/1000`.
- A cancellation-aware regrouping bounds the second-row cubic coefficient by
  `166112` and the complete base tail by `166200`, replacing `837000`. Together
  with the unchanged `3301` high-core estimate, this keeps the second face pair
  strict on the enlarged radius.
- Sharpening the retained signed affine bounds to
  `cbase <= 56887671/50`, `c0 <= 201407/10`, `c1 <= -938811/5`,
  and `-936971/1000 <= c2` bounds the cleared-error cubic coefficient by
  `736875`.
- The retained `frozenAlgKMajorant` is bounded by `161/10` at radius `1/10`.
  The resulting scalar bounds for the fold, cosine, area, and `qPrime`
  majorant DAGs are `29/2`, `9553/5`, `160443/50`, and `83/4`, giving the
  frozen third-row numerator bound `6750317/600`. The retained
  `frozenPathRhoNumeratorMajorant` has the exact scalar bound `1313/1250`, and
  the common-denominator majorant is bounded by `43923/5120`, sharpening the
  former `429/50` bound. Propagating these without integer widening gives exact
  four-bar and three-bar bounds `26379901/50000` and `2661239/200`. The retained
  `frozenPathPiCoefficientMajorant` is bounded by `64883/25`. After the common
  denominator, the complete cleared-error majorant at radius `1/10` is at most
  `7870625783673/800000`, giving the exact fourth-tail bound
  `7870625783673/80`; its rounded physical tail on `1/126334` is `778752` and
  its cleared cubic quotient is `1515627`, with frozen-row slack
  `366813/23198080750`.
- The endpoint-box density cube quotient is kernel-proved to lie strictly
  between `251/20` and `14` on every positive slice below `1/100`.
- The first explicit prescribed-density cell proves negative type-(iii) fold,
  negative reduced fold gap, and strict perimeter improvement for every
  `1 < lambda < 1 + (2/200000)^3 = 1 + 1/10^15`.
- An adjacent fixed-prism Poincare-Miranda theorem covers the first seam and
  proves the same conclusions for
  `1 + (2/200000)^3 <= lambda <=
  1 + (251/20)*(1/200000)^3`, with
  `1/400000 < s < 1/200000`.
- A second fixed prism covers that exact closed endpoint and proves the same
  conclusions through
  `1 + (251/20)*(1/180000)^3`, with
  `1/360000 < s < 1/180000`.
- A third fixed prism covers that exact closed endpoint and proves the same
  conclusions through
  `1 + (251/20)*(1/179668)^3`, with
  `1/359336 < s < 1/179668`.
- A fourth fixed prism covers that exact closed endpoint and proves the same
  conclusions through
  `1 + (251/20)*(1/170000)^3`, with
  `1/340000 < s < 1/170000`.
- A fifth fixed prism covers that exact closed endpoint and proves the same
  conclusions through
  `1 + (251/20)*(1/168000)^3`, with
  `1/336000 < s < 1/168000`.
- A sixth fixed prism covers that exact closed endpoint and proves the same
  conclusions through
  `1 + (251/20)*(1/167500)^3`, with
  `1/335000 < s < 1/167500`.
- A seventh fixed prism covers that exact closed endpoint and proves the same
  conclusions through
  `1 + (251/20)*(1/167401)^3`, with
  `1/334802 < s < 1/167401`.
- An eighth fixed prism covers that exact closed endpoint and proves the same
  conclusions through
  `1 + (251/20)*(1/164000)^3`, with
  `1/328000 < s < 1/164000`.
- A ninth fixed prism covers that exact closed endpoint and proves the same
  conclusions through
  `1 + (251/20)*(1/163935)^3`, with
  `1/327870 < s < 1/163935`.
- A tenth fixed prism covers that exact closed endpoint and proves the same
  conclusions through
  `1 + (251/20)*(1/161000)^3`, with
  `1/322000 < s < 1/161000`.
- An eleventh fixed prism covers that exact closed endpoint and proves the same
  conclusions through
  `1 + (251/20)*(1/160900)^3`, with
  `1/321800 < s < 1/160900`.
- A twelfth fixed prism covers that exact closed endpoint and proves the same
  conclusions through
  `1 + (251/20)*(1/160829)^3`, with
  `1/321658 < s < 1/160829`.
- A thirteenth fixed prism covers that exact closed endpoint and proves the same
  conclusions through
  `1 + (251/20)*(1/160817)^3`, with
  `1/321634 < s < 1/160817`.
- A fourteenth fixed prism covers that exact closed endpoint and proves the same
  conclusions through
  `1 + (251/20)*(1/160778)^3`, with
  `1/321556 < s < 1/160778`.
- A fifteenth fixed prism covers that exact closed endpoint and proves the same
  conclusions through
  `1 + (251/20)*(1/160777)^3`, with
  `1/321554 < s < 1/160777`.
- A sixteenth fixed prism covers that exact closed endpoint and proves the same
  conclusions through
  `1 + (251/20)*(1/160772)^3`, with
  `1/321544 < s < 1/160772`.
- A seventeenth fixed prism covers that exact closed endpoint and proves the
  same conclusions through
  `1 + (251/20)*(1/160771)^3`, with
  `1/321542 < s < 1/160771`.
- An eighteenth fixed prism covers that exact closed endpoint and proves the
  same conclusions through
  `1 + (251/20)*(1/160770)^3`, with
  `1/321540 < s < 1/160770`.
- A nineteenth fixed prism covers that exact closed endpoint and proves the
  same conclusions through
  `1 + (251/20)*(1/160769)^3`, with
  `1/321538 < s < 1/160769`.
- A twentieth fixed prism covers that exact closed endpoint and proves the same
  conclusions through
  `1 + (251/20)*(1/160545)^3`, with
  `1/321090 < s < 1/160545`.
- A twenty-first fixed prism covers that exact closed endpoint and proves the
  same conclusions through
  `1 + (251/20)*(1/138170)^3`, with
  `1/276340 < s < 1/138170`.
- A twenty-second fixed prism covers that exact closed endpoint and proves the
  same conclusions through
  `1 + (251/20)*(1/130730)^3`, with
  `1/261460 < s < 1/130730`.
- A twenty-third fixed prism covers that exact closed endpoint and proves the
  same conclusions through
  `1 + (251/20)*(1/128797)^3`, with
  `1/257594 < s < 1/128797`.
- A twenty-fourth fixed prism covers that exact closed endpoint and proves the
  same conclusions through
  `1 + (251/20)*(1/128788)^3`, with
  `1/257576 < s < 1/128788`.
- A twenty-fifth fixed prism covers that exact closed endpoint and proves the
  same conclusions through
  `1 + (251/20)*(1/128489)^3`, with
  `1/256978 < s < 1/128489`.
- A twenty-sixth fixed prism covers that exact closed endpoint and proves the
  same conclusions through
  `1 + (251/20)*(1/128399)^3`, with
  `1/256798 < s < 1/128399`.
- A twenty-seventh fixed prism covers that exact closed endpoint and proves the
  same conclusions through
  `1 + (251/20)*(1/128394)^3`, with
  `1/256788 < s < 1/128394`.
- A twenty-eighth fixed prism covers that exact closed endpoint and proves the
  same conclusions through
  `1 + (251/20)*(1/128389)^3`, with
  `1/256778 < s < 1/128389`.
- A twenty-ninth fixed prism covers that exact closed endpoint and proves the
  same conclusions through
  `1 + (251/20)*(1/128385)^3`, with
  `1/256770 < s < 1/128385`.
- A thirtieth fixed prism covers that exact closed endpoint and proves the same
  conclusions through
  `1 + (251/20)*(1/128384)^3`, with
  `1/256768 < s < 1/128384`.
- A thirty-first fixed prism covers that exact closed endpoint and proves the
  same conclusions through
  `1 + (251/20)*(1/127280)^3`, with
  `1/254560 < s < 1/127280`.
- A thirty-second fixed prism covers that exact closed endpoint and proves the
  same conclusions through
  `1 + (251/20)*(1/126341)^3`, with
  `1/252682 < s < 1/126341`.
- A thirty-third fixed prism covers that exact closed endpoint and proves the
  same conclusions through
  `1 + (251/20)*(1/126334)^3`, with
  `1/252668 < s < 1/126334`.
- The public theorem `prescribedDensity_lambda_explicit_combined` composes all
  thirty-four cells, so the explicit interval is
  `1 < lambda <= 1 + (251/20)*(1/126334)^3`.
- Round 192's scale-local numerical pilot replayed both complete closed bands
  `delta in [(251/20)*(1/126334)^3, 8*(251/20)*(1/126334)^3]` and
  `delta in [1/1000, 1/500]`, where `delta = lambda - 1`, with 64 rational
  subslabs per band. Every subslab passed eight uniform mean-value face
  enclosures, principal-chart and denominator guards, polynomial height order,
  and negative normalized gap using outward-rounded 160-bit dyadic arithmetic.
  Inputs, exact margins, reproduction receipts, and the explicit soundness/
  Lean-replay path are retained in `proof/scale_local_atlas/REPORT.md`.
  These are numerical pilot certificates, NOT new kernel-checked coverage.
- Round 210 extended the same exact checker to the complete thirteen-band
  target manifest
  `[8^j*delta0, min(8^(j+1)*delta0,1/1000)]`, with
  `delta0=(251/20)*(1/126334)^3`, scale `2^j/126334`, and 64 closed rational
  subslabs for each `j=0,...,12`. All 832 cells passed the unchanged numerical
  face, guard, gap, preconditioner, adjacency, and analytic-remainder-domain
  checks; the prescribed scale is now checked in every cell. Inputs,
  enclosures, exact margins, resource measurements, and hashes are retained in
  `proof/scale_local_atlas/target-preflight-receipt.json`. This is a numerical
  preflight, NOT kernel-checked density coverage.
- `NearOneLocalTranscription` kernel-checks the five-coordinate enclosure as a
  telescope of five actual scalar mean-value applications, the exact
  polynomial-plus-`R3` arctangent quotient and its actual derivative with the
  analytic tail retained, and
  `regularizedThirdRow/s^2 = completeThirdRowNumerator/s^4` under nonzero scale
  and the three source denominator guards. The fixed generated-third
  transcription and its value and five slice-derivative interval replays on
  the seam and remote cells remain pending, so this adds no density coverage.
- Round 213's execution-209 two-scale semantic gate is falsified. The retained
  `NearOneLocalGeneratedThird` still ends at the algebraic-only
  `algebraicThirdDivided_value`; the complete emitted source identity and both
  cells' uniform value/five actual derivative replays are absent. A bounded
  direct source check failed with Lean's `memory_exception` at a 9000 MB limit
  after 17.36 seconds, with peak RSS 9218152 KiB and no cgroup OOM kill.
  `NearOneLocalJet` passed a direct source check, and a focused audit of six
  jet declarations and three shared transcription declarations reported only
  the allowed axioms. These partial checks do not pass the gate. Strategy
  `strategy-00037` is terminated without restarting its clock; the independent
  compatibility proposal remains below its authorization threshold. Commands,
  logs, missing obligations, and incomplete cumulative CPU accounting are
  retained in round 213's `semantic-gate-recovery.json`. No density coverage
  was added.
- Round 214's centered-difference replacement kernel-checks exact finite-coordinate
  secant semantics for constants, coordinates, addition, negation,
  multiplication with `D_f*V_g + V_f*D_g`, scalar analytic composition,
  positive reciprocal, outward widening, and uniform boxes. A small source-side
  barrier identifies `sourceGroupedThird/s^2` with
  `completeThirdRowNumerator/s^4`; on target band 0 cell 0, the three existing
  denominator certificates and positive scale instantiate that identity at
  every box point. Focused audits report only the allowed axioms. The checker
  now optionally retains center, whole-box, and five centered coefficients, and
  `proof/scale_local_atlas/centered-gate-data.json` records all 23 outputs plus
  four actual remainder arguments for both fixed gate cells. These are inputs,
  not Lean interval proofs. The complete emitted-CSE identity, the two uniform
  third enclosures, and the remote source guards remain absent. During this
  work the inherited cgroup recorded `memory.peak = 11782295552` bytes, exceeding
  strategy-00038's 10 GiB aggregate-memory limit of `10737418240` bytes.
  Therefore its explicit resource kill criterion is triggered and
  `strategy-00038` is terminated; no density coverage was added.
- Round 215 established strategy-00039's acceptance-scoped resource controls:
  the inherited service cgroup now enforces `memory.max = 10737418240` bytes
  with swap disabled. Its successful focused checks reached that exact ceiling,
  causing reclaim events but no OOM or OOM kill; `90` cgroup CPU seconds
  are conservatively charged to the strategy. `NearOneLocalFourRemainder`
  kernel-checks the exact actual-source decomposition into the four retained
  `R3` values, joint affinity in those values, the exact
  midpoint-plus-analytic-error identity, and positivity of the frozen common
  clearing factor. Focused audits report only the allowed axioms. The exact two
  sample manifests, clearing-factor list, certificate format, resource
  snapshots, and hashes are retained in round 215's
  `strategy-00039-milestone-1.json`. The two cells' scalar remainder ranges,
  error budgets, polynomial witnesses, and sixteen source-connected face signs
  remain pending, so no density coverage was added.
- Round 216 kernel-checks a reusable exact-rational `N=20` Horner enclosure
  for `R3` and a two-cell `NearOneLocalRemainderGate`. On the retained seam
  cell and remote stress cell, the new module proves all four actual remainder
  argument intervals, all four corresponding analytic `R3` intervals, and
  explicit midpoint-radius error budgets; it also proves the remote positive
  scale and three source denominator guards and instantiates the exact
  source-to-complete-third identity. Focused builds pass, and audits report only
  `propext`, `Classical.choice`, and `Quot.sound`. The positively scaled
  polynomial-plus-error face identities and all sixteen source-connected face
  signs are still absent. Execution 212 therefore misses strategy-00039's
  milestone-2 deadline and triggers its explicit deadline and missing-face
  kill criteria. Strategy `strategy-00039` is terminated without a clock
  restart; no density coverage was added. The inherited cgroup remained under
  its enforced 10 GiB ceiling with no OOM event. Exact resource snapshots,
  hashes, completed declarations, and missing obligations are retained in
  round 216's `strategy-00039-milestone-2.json`.
- Round 217 accepts the independent source-native scalar strategy under a fresh
  cgroup. The current cgroup enforces `memory.max = 10737418240` bytes with swap
  disabled; milestone-1 work used 99.451951 measured cgroup CPU seconds, peaked
  at 5039357952 bytes, and caused no memory event. `NearOneScalarNormalization`
  kernel-checks the strict source guards, the two exact scaled radicals, the
  principal arctangent branch identity, and the exact `F`, `E`, and `J`
  connections to the actual `typeFourFold`, scaled type-(iii)-minus-type-(iv)
  area, and `reducedFoldGap`. `NearOneScalarGrid` fixes the exact 13-by-64
  target grid, proves its 832-cell count and endpoint adjacency, and checks the
  seam, three weak-region cells, and remote stress slab by exact rational cube
  comparisons. Focused builds pass, and the aggregate audit reports only
  `propext`, `Classical.choice`, and `Quot.sound`. The moving brackets and
  complexity caps are frozen in round 217's
  `strategy-00040-milestone-1.json`. No numerical scalar preflight, formal
  stress-sample exclusion, or density coverage has yet been produced.
- Round 218 completes strategy-00040 milestone 2 within its execution-214
  deadline. A fresh standalone source-native scalar preflight checks exactly the
  frozen 13-by-64 `t` cells plus the single remote slab, using 160-bit outward
  dyadic arithmetic, degree-1 moving predictors
  `a=p4(t)+t^2*epsilon4`, `b=p3(t)+t^2*epsilon3`, and the exact seven-term
  arctangent quotient with its retained analytic tail. All 833 rectangles have
  strict opposite `F` faces, strict opposite `E` faces uniformly over the
  complete `a` bracket, whole-rectangle `J < 0`, and all 44 source,
  denominator, branch, sign-transfer, and height-order guards. The complete
  output also retains four square-checked radical witnesses, eight
  product-checked reciprocal witnesses, exact Machin `pi`, and every analytic
  error budget. The weakest exact fold, area, gap, and guard margins occur at
  band-0 cells 33, 59, 0, and 0 respectively and remain strictly positive.
  An independent audit re-derives the inventory, validates every saved bound
  and budget, evaluates the literal source formulas at 29,988 diagnostic
  points, and passes nine fail-closed mutations. Generation, replay, and audit
  stayed under the accepted complexity and per-cell cost caps; the cgroup
  remained below its enforced 10 GiB ceiling with no memory event. Exact
  artifacts and resource accounting are retained in round 218's
  `strategy-00040-milestone-2.json`. This is numerical preflight evidence, not
  a Lean interval proof, formal stress-sample exclusion, or density coverage.
- Round 219 falsifies strategy-00040 at its compulsory formal stress gate.
  Five 1119-line generated stress modules encode the frozen band-0 cell 10,
  band-10 cells 32--34, and remote slab; an independent static audit found
  their rectangles and source-level `F`, `E`, `J`, nested-IVT, height-order,
  gap, and unchanged candidate-consumer connections semantically aligned with
  the manifest. They are not Lean proofs: the first direct source check reports
  a parser error at the doc-comment preceding `namespace Band0Cell10` and an
  unsolved first `widenAll` enclosure goal at line 38. Before termination that
  single-cell check had consumed 2:52 CPU at 2:42 elapsed time and had
  7115460 KiB RSS, exceeding the strategy's explicit 60 CPU-second
  complete-cell checking cap. The required execution-215 five-sample gate is
  therefore absent and its deadline and cost kill criteria are triggered.
  Strategy `strategy-00040` is terminated without a clock restart. The broken
  stress aggregator was removed from the registered support target and
  assumptions audit; the reusable `NearOneScalarInterval` and
  `NearOneScalarCell` modules still build and their focused audit reports only
  `propext`, `Classical.choice`, and `Quot.sound`. Exact diagnostics, hashes,
  cleanup, and resource observations are retained in round 219's
  `strategy-00040-milestone-3.json`. No formal stress exclusion or density
  coverage was added.
- Round 220 falsifies strategy-00041 at its compulsory A+1 performance gate.
  Acceptance-scoped accounting began only after the fresh cgroup's inherited
  12 GiB ceiling was reduced to 10 GiB with swap disabled. A fixed five-domain
  signed-integer trace exactly reproduces all four retained faces, the gap, and
  44 guards for band-0 cell 10 using 17,758 proof-free scalar interval steps;
  the shared checker validates backward references, the frozen operation
  schedules and output slots, rational inputs, arithmetic operations,
  sign-sensitive powers, square roots, positive reciprocals, analytic-tail
  budgets, intersections, and final signs with integer inequalities. The
  shared checker and frozen program build, and its basic integer-to-real
  boundary uses only the allowed axioms. The complete trace theorem did not
  kernel-check: ordinary `decide` remained unreduced after 59.45 user and 5.28
  system CPU seconds, 32.64 elapsed seconds, and 7732676 KiB peak RSS. This
  exceeds the explicit 30 CPU-second A+1 prerequisite, so strategy
  `strategy-00041` is terminated. The generated cell module is retained only
  as diagnostic data and is excluded from registered build roots. No formal
  cell exclusion or density coverage was added.
- Round 221 completes strategy-00042 milestones 1 and 2. Under a fresh
  acceptance-scoped 10 GiB cgroup with swap disabled,
  `SameCurvatureArea` kernel-checks the actual support derivative
  `T' = -2*typeFourDelta`, closed-interval concavity, the strict endpoint and
  midpoint inequalities, and the resulting strict same-height `P-hA`
  comparison. It also proves that an increasing type-(iii) fold bounds every
  intermediate area by the larger endpoint area and uses the actual
  variational identity to prove the universal equal-area transfer
  `typeThreePerimeter_lt_typeFourPerimeter_of_equalArea`. The final theorem has
  only `1 < lambda`, `1/2 < r < h < 1`, and actual equal area as hypotheses.
  The focused build and final-declaration audit pass with only `propext`,
  `Classical.choice`, and `Quot.sound`. Exact resource accounting and hashes
  are retained in round 221's `strategy-00042-milestones-1-2.json`. No
  stationary pair existence or density coverage was added.
- Round 222 completes strategy-00043 milestone 1. `TypeFourStationaryRoot`
  kernel-checks the denominator-free continuous extension of the type-(iv)
  fold on `[9/10,1]`, its positive regular-domain identity with the actual
  source fold, strict negative and positive endpoint signs, and
  `typeFourFold_zero_exists`: for every `lambda > 1` there is
  `9/10 < h < 1` with the actual `typeFourFold lambda h = 0`. The right
  endpoint proof uses only the cleared expression, never the totalized
  singular fold. The focused build, final-declaration audit, and aggregate
  assumptions audit pass with only `propext`, `Classical.choice`, and
  `Quot.sound`. No stationary equal-area pair or density coverage was added.
- Round 223 completes strategy-00043 milestone 2. `UniversalStationaryPair`
  proves `A₄(h₄) <= A₄(1) < A₃(1/2)` at every stationary root supplied by
  `TypeFourStationaryRoot`, applies the actual continuous type-(iii) area to
  obtain `1/2 < h₃ < h₄ < 1` with equal area, derives negative type-(iii)
  fold, strict perimeter improvement, and negative reduced fold gap, and feeds
  the unchanged candidate consumer. The resulting modeled candidate exclusion
  holds for every `lambda > 1`; source classification and model compatibility
  remain explicit. The focused build and final-declaration audit pass with only
  `propext`, `Classical.choice`, and `Quot.sound`. Exact resource accounting and
  hashes are retained in round 223's `strategy-00043-milestone-2.json`.
- `NearOneLocalHeightOrder.height_order_iff` and
  `NearOneLocalSource.stationaryEqualAreaPair_strictImprovement` are
  kernel-checked with only the allowed axioms. They transport explicit local
  source equations, height order, and cleared-gap negativity to the existing
  scalar consumer without an endpoint-box or `s < 1/100` hypothesis.
  They do not discharge the pilot's interval bounds or generated identities.
- `NearOneAtanRemainder` kernel-checks the exact generic arctangent-remainder
  recurrence, finite expansion, integral derivative, global value/derivative
  estimates, and both uniform analytic-tail bounds. Its retained `k=3`,
  `N=20` specializations are exactly `B^40/47` and
  `40*B^39/47 + 2*B^41/49`. The four pilot argument enclosures and generated
  mean-value replay remain pending, so this adds no density coverage.
- `NearOneLocalInterval` kernel-checks a shared outward-rounded multiplication
  soundness lemma and the exact 160-bit dyadic `pi` interval used by the pilot,
  from Machin's identity and the retained 36-term arctangent remainder bound.
- `NearOneLocalPredictor` kernel-checks reusable exact-rational affine-predictor
  and scaling enclosure soundness. For the seam-adjoining pilot subslab
  `251/20 <= mu <= 17821/1280`, it replays the saved 160-bit `s_pos`,
  `s_lt_one`, `y_pos`, `y_lt_one`, `w_pos`, `w_lt_one`, `v_pos`, `v_lt_one`,
  `y_gt_s`, `v_gt_w`, `density_den`, `angle_den_four`, `angle_den_three`,
  `atan_guard_four`, `atan_guard_three`, `height_order`, and `A_pos` endpoints
  from the actual `tau`, `u`, `v`, and `w` affine predictors and corrected
  nonlinear coordinates. The dependency-aware identities `Y - s = s^2*z` and
  `V - W = s^2*e` prove `0 < s < Y < 1` and `0 < W < V < 1`. Exact
  positive-denominator division proves the two generated arctangent-addition
  guards. Monotonicity of `(1-s^2)*(2+s*a)^2-2` on the certified local range
  replays the positive height margin and transports it through
  `NearOneLocalHeightOrder.height_order_iff`. The exact `A = 1+s*z` interval
  has a strictly positive retained lower endpoint. The remaining `gap_den`
  guard, generated-expression/source identities, face signs, gap, and
  Poincare-Miranda step remain pending, so this adds no density coverage.
- `CMVRelaxation` kernel-checks the source-located extended-valued relaxation
  over open domains with local one-sided regular `C-infinity` defining
  functions. It identifies symmetric-difference volume with the extended `L1`
  characteristic-function distance; gives an invertible arbitrary-horizontal-
  translation transport of every smooth sequence, preserving global
  convergence and each extended cost; and supplies a positive-volume smooth
  unit disk with finite smooth cost and a constant admissible sequence. The
  relaxed functional itself is exactly invariant under arbitrary horizontal
  translation and almost-everywhere replacement, including when its value is
  infinite. The concrete `relaxedSourceSemantics` adapter defines perimeter
  finiteness as null measurability plus finite relaxed perimeter and converts
  to a real value only behind finite-value lemmas. The source minimizer domain
  now also requires weighted-area integrability, matching CMV's finite weighted
  volume semantics on printed pages 2--3 and preventing the real integral's
  non-integrable fallback from entering equal-area comparison. Every modeled
  competitor and every canonical regular type-(iv) profile is kernel-proved to
  have finite weighted area. For every source-admissible canonical profile
  classified by either exact or almost-everywhere horizontal congruence, the
  concrete adapter constructs the existing `NormalizationWitness`, including
  weighted-area transport, without compatibility, frontier equality, a
  caller-supplied witness, or a caller-supplied approximating sequence. The
  existing minimizer-transfer consumer and modeled `lambda >= 51/50` cutoff are
  exercised directly from both classifications without supplied normalization;
  compatibility and universal classification remain explicit. This adds no
  unconditional density coverage.
- `CMVSourceClassification` starts from an independent common radius, exterior
  half-angle, horizontal placement, and explicit five-piece closed source
  carrier. From strict regularity and Snell's law it kernel-derives the
  canonical principal-angle `CanonicalTypeIVProfile`/`FourArcCandidate`, exact
  centered-carrier equality, and exact and almost-everywhere horizontal
  congruence. The Figure-4 scalar reduction from Lemma 3.8 Step 2 now derives
  equal strip-angle sines and the relation `sin α = sin β = 1/R` from equations
  (24)--(25) and the vertical-axis center alignment; horizontal symmetry is no
  longer merely an input at that scalar interface. The raw carrier feeds the
  normalization-free relaxed cutoff directly under the still-explicit
  `CompatibleWithModel` premise. The
  universal GMT theorem that every source-admissible regular type-(iv)
  minimizer is almost everywhere one of these raw carriers remains unresolved,
  so this adds no unconditional density coverage.
- `CMVSourceSectionClassification` kernel-checks the measure-theoretic
  reconstruction step matching CMV Proposition 3.6 and Lemma 3.8. The raw
  five-piece carrier is closed and Borel measurable. Its strict strip slices
  are exactly one centered interval: the checked radius attachment inequality
  rules out gaps between the two side disks and central rectangle. Every
  non-interface slice is kernel-proved empty or a centered interval, including
  both exterior cap regimes. Consequently, centered interval slices for a
  source representative plus equality of each one-dimensional slice measure
  imply `HasTypeIVHorizontalSections`; this uses interval-volume uniqueness,
  not planar equality or congruence. The checked Fubini bridge then gives planar
  almost-everywhere equality. Deriving centered interval slices and slice
  equimeasurability for every source type-(iv) minimizer from Schwarz
  symmetrization, its equality case, regularity, arc continuation, Snell laws,
  and exclusion of the other Lemma 3.8 configurations remains unresolved.
  This adds no unconditional density coverage.
- Round 226 completes strategy-00044 milestone 1. `CMVProjectionDefect` proves
  the general collar-based projection-defect estimate for every open
  approximant using Euclidean `H¹` after `planeEuclideanHomeomorph`, converts
  weighted projection width into `smoothCost`, and passes the defect estimate
  through the actual extended liminf of every convergent `SmoothSequence`.
  For the actual canonical profile `lambda = 2`, `h = 1/2`, it proves the
  prescribed lower-collar inclusion, upper-collar disjointness, and density-two
  box, hence
  `1 <= smoothCost 2 U + 16 * characteristicDistance U profile.carrier`
  for every open `U` and `1 <= A.cost 2` for every convergent smooth sequence.
  The focused build and declaration audit pass with only `propext`,
  `Classical.choice`, and `Quot.sound`. This is the A+2 lower gate only; the
  independent A+4 smooth recovery family, universal box exhaustion, and
  canonical relaxed-perimeter equality remain unresolved, so it adds no
  unconditional density coverage.
- Round 227 advances strategy-00044 milestone 2. The relaxation's
  `IsSmoothDomain` contract now uses mathlib's actual `ContDiff ... ∞`
  smoothness rank; the former `⊤` denotes the stronger analytic rank `ω` in
  the current mathlib and blocked every flat-cutoff recovery. The existing
  translation and unit-disk witnesses still kernel-check under the corrected
  contract. `CMVSquaredWidthRecovery` defines a global smooth frozen-profile
  squared width for every `0 < epsilon <= 1/16`, proves exact side/cap
  plateaus, the only two zeros `y = +/-5/2`, pole derivatives `+/-4`, positive
  connected vertical support, and one global `IsSmoothDomain` witness covering
  both poles and all four transition endpoints. The side and cap squares are
  both exactly `15/4` at `y = +/-1`, and a reusable checked quotient estimate
  records the required junction-value cancellation without dropping a hidden
  `1/epsilon` factor. The focused build and declaration audit pass with only
  `propext`, `Classical.choice`, and `Quot.sound`. Identification with the
  actual carrier modulo frontier, the global characteristic-error estimate,
  and the weighted-cost estimate remain pending, so milestone 2 is advanced
  but not complete and no density coverage is added.
- Round 228 further advances strategy-00044 milestone 2.
  `CMVSquaredWidthRecovery` now identifies the actual frozen five-piece carrier
  exactly as `p.1 ^ 2 <= targetQ p.2`. Its open strict-sublevel representative
  is continuous and agrees with that closed carrier almost everywhere; the
  intervening squared-width level has planar volume zero by a checked
  horizontal-fiber argument. Outside the two junction bands, the recovery
  squared width equals `targetQ`. The symmetric difference is therefore
  contained in two width-six, height-`epsilon` rectangles plus a null level
  set, yielding the global checked bound
  `characteristicDistance (FrozenSquaredRecovery.domain epsilon)
  profile.carrier <= 12 * ENNReal.ofReal epsilon`. The concrete scales
  `1 / (n + 16)` package these domains into an actual `SmoothSequence` whose
  characteristic distance to `profile.carrier` tends to zero. The complete
  recovery frontier is additionally contained in two compact square-root graph
  traces, exposing the geometric interface needed for the weighted-cost bound.
  The focused build and
  declaration audit pass with only `propext`, `Classical.choice`, and
  `Quot.sound`. The epsilon-uniform weighted-cost estimate remains pending, so
  milestone 2 is advanced but not complete and no density coverage is added.
- Round 229 completes strategy-00044 milestone 2. The four shrinking junction
  bands are compactified in normalized `(epsilon, t)` coordinates whose cutoff
  argument has denominator `2 - epsilon`, so compact `C¹` control gives
  epsilon-independent Lipschitz constants. A separate checked quotient theorem
  applies the exact side/cap value cancellation at `y = +/-1` and bounds
  `(capSquare y - sideSquare y) / epsilon` on both junction bands without a
  hidden `1/epsilon` loss. Eight normalized Lipschitz curves cover the complete
  Euclidean recovery frontier: two fixed side traces, four junction traces, and
  two fixed radius-two circles. Hausdorff-measure subadditivity and the pointwise
  density bound `StripDensity 2 <= 2` then prove one finite `C`, independent of
  `epsilon`, with `smoothCost 2 (FrozenSquaredRecovery.domain epsilon) <= C`
  whenever `0 < epsilon <= 1/16`. Together with rounds 227--228, this completes
  the frozen A+4 smoothness, characteristic-convergence, and uniform weighted-
  cost gate. The focused build and declaration audit pass with only `propext`,
  `Classical.choice`, and `Quot.sound`; no density coverage is added.
- Round 230 kernel-checks the rigid-coordinate and finite-summation primitives
  needed by the projection lower bound. `CMVRigidProjection` conjugates every
  Euclidean isometry back to the coordinate plane, proves exact planar-volume,
  characteristic-distance, and local Euclidean `H¹` transport, and derives a
  localized weighted projection defect. `RigidProjectionPatch` packages the
  collar and density certificates, and
  `finset_sum_payoff_le_smoothCost_add_error` proves that pairwise-disjoint
  windows charge one global smooth cost rather than duplicating it once per
  box. The focused build and declaration audit pass with only `propext`,
  `Classical.choice`, and `Quot.sound`. The actual four-arc canonical box
  exhaustion was not completed by execution 226, and the inherited cgroup has
  `memory.max = 12884901888` rather than the strategy's compulsory
  `10737418240` bytes, so both the milestone deadline and acceptance-scoped
  containment kill criteria are triggered. Strategy `strategy-00044` is
  terminated; no universal lower bound, relaxed-perimeter equality, or density
  coverage was added.
- Round 238 completes strategy-00047 milestone 2 after prospectively tightening
  its own inherited cgroup to `memory.max = 10737418240` with swap disabled and
  reconciling round 237's completed successful receipt with its original
  request and admitted campaign unit. `CMVRightSideExhaustion` kernel-checks,
  for every canonical profile and `eta > 0`, a finite family of actual right
  circular-side `RigidProjectionPatch` values. Every complete window lies in
  `x > outerHalfWidth` and `|y| < 1`, has exact weight one, and the windows are
  pairwise disjoint; the summed error coefficient is finite, the payoff is
  within `eta` of the complete right-side arclength, and the existing finite
  summation theorem bounds that payoff by one global `smoothCost` plus one
  characteristic-distance error. The focused support build and declaration
  audit pass with only `propext`, `Classical.choice`, and `Quot.sound`.
  Reflection to the left and lower arcs, four-family cross-disjointness, and
  the universal relaxed-perimeter lower bound remain pending, so no density
  coverage was added.
- Round 239 completes strategy-00047 milestone 3. `CMVFourArcExhaustion`
  defines exact horizontal- and vertical-axis Euclidean reflections, proves
  that both preserve the actual canonical carrier and strip density, and
  transports every upper-cap and right-side `RigidProjectionPatch` to the
  lower cap and left side without changing payoff or error coefficient. The
  four families occupy the strict regions `y > 1`, `y < -1`,
  `x > outerHalfWidth` with `|y| < 1`, and `x < -outerHalfWidth` with
  `|y| < 1`; they are kernel-proved pairwise disjoint both within and across
  families. Their four target contributions are identified exactly with
  `ENNReal.ofReal` of the actual complete-frontier weighted perimeter, and for
  every positive deficit their combined payoff is charged through one
  application of `finset_sum_payoff_le_smoothCost_add_error`. The focused
  support build passes, the declaration audit reports only `propext`,
  `Classical.choice`, and `Quot.sound`, and the admitted cgroup stayed below
  its 10 GiB ceiling without memory events. Removing the arbitrary deficit
  against every convergent `SmoothSequence` remains pending, so no relaxed-
  perimeter equality or density coverage was added.
- Round 240 kernel-checks the remaining universal canonical lower bound.
  `SmoothSequence.cost_add_ge_of_pointwise_add_distance` now handles arbitrary
  density, target, finite error coefficient, and fixed additive allowance; the
  frozen projection consumer is migrated to this generalized lemma.
  `CMVCanonicalLowerBound` applies the four-arc exhaustion at each positive
  deficit, removes its finite characteristic-distance error along every actual
  convergent `SmoothSequence`, removes the arbitrary deficit, and proves
  `CanonicalFourArc.frontierCost_le_relaxedPerimeter` for every canonical
  profile. The focused support build passes and both audits report only
  `propext`, `Classical.choice`, and `Quot.sound`. The proof work and initial
  direct checks nevertheless preceded prospective tightening of round 240's
  distinct inherited cgroup: its initial `memory.max` was `12884901888`, not
  `10737418240`. Tightening afterward cannot cure that explicit protocol
  violation, so strategy `strategy-00047` is terminated and milestone 4 is not
  accepted under its resource contract. The kernel-checked declarations remain
  valid retained mathematics. No upper recovery bound, relaxed-perimeter
  equality, source compatibility, or density coverage was added.
- Round 241 completes strategy-00048 milestone 1. The invocation's first
  execution operation identified and prospectively tightened its actual
  inherited cgroup from 12 GiB to 10 GiB with swap disabled, verified a
  foreground child in the same cgroup, and refused a deliberately mismatched
  invocation identity with exit code 73 without entering the sentinel payload.
  `CMVSharpRecovery` then proves that exact attachment cancellation makes the
  normalized frozen squared width Lipschitz in the trace parameter with
  constant `K * epsilon`, retains the lower bound `15/4` through square root,
  instantiates all four actual `recoveryCoverCurve` junction branches, and
  bounds each branch's Euclidean `H¹` by `K_i * epsilon`. The focused support
  build passes and the declaration audit reports only `propext`,
  `Classical.choice`, and `Quot.sound`. Actual-frontier domination, a sharp
  frozen weighted-cost bound, and its consumption in the relaxed upper bound
  remain pending, so strategy milestone 2 is advanced but incomplete and no
  relaxed-perimeter equality or density coverage was added.
- Round 242 completes strategy-00049 milestone 2 under prospective
  same-invocation admission: the actual inherited cgroup was tightened from
  12 GiB to 10 GiB with swap disabled before mathematics, a foreground child
  remained in that cgroup, and the mismatched-identity sentinel was refused.
  `CMVSharpRecovery` kernel-checks literal Euclidean frontier domination for
  the frozen recovery by the actual canonical frontier plus the four moving
  junction traces; the proof treats the junction endpoints and both poles
  through the exact squared-width level. It preserves the canonical weighted
  frontier integral, charges only the junction union by a coarse density bound,
  and proves one finite scale-independent `C` with recovery cost at most the
  actual canonical extended frontier cost plus `C * epsilon`. The existing
  `recoverySequence` consumes this estimate in the literal `relaxedPerimeter`
  upper bound; combined with the retained universal lower bound, the frozen
  profile now has exact extended relaxed-perimeter equality. The focused
  support build passes and the declaration audit reports only `propext`,
  `Classical.choice`, and `Quot.sound`. Universal profilewise recovery remains
  pending, so this adds no unconditional density coverage.
- Round 243 completes strategy-00050 milestone 1 under prospective
  same-invocation admission. `CMVProfileSquaredWidth` works over an arbitrary
  `profile : CanonicalTypeIVProfile lam`: it constructs a smooth global
  side-radical cutoff that is exact on `|y| <= 1` and stays strictly below the
  profile radius square, proves that the side and cap squared widths have the
  same positive attachment value at both interfaces, and constructs the
  positive scale `min (1/16) (1 - capCenter)`. On every resulting junction
  collar the actual convex blend is bounded below by the positive attachment
  square. The actual five-piece closed carrier is identified exactly with the
  non-strict piecewise squared-width sublevel at every point, including both
  interfaces and both poles. The focused support build passes and the
  declaration audit reports only `propext`, `Classical.choice`, and
  `Quot.sound`. A universal smooth recovery sequence, characteristic
  convergence, and profilewise relaxed-perimeter equality remain pending, so
  no unconditional density coverage is added.
- Round 244 completes strategy-00050 milestone 2 under prospective
  same-invocation admission. For every
  `profile : CanonicalTypeIVProfile lam`, `CMVProfileSquaredWidth`
  kernel-checks that the blended squared width has exactly the two cap poles
  `y = +/- (radius + capCenter)` as zeroes, with derivatives `+/- 2 * radius`,
  and therefore defines one global `IsSmoothDomain` at every positive scale
  through the internally constructed `safeScale`. A profile-dependent finite
  horizontal radius contains the recovery and target sections, giving the
  global characteristic-distance estimate
  `distance <= 4 * ofReal horizontalRadius * ofReal epsilon`.
  The actual `SmoothSequence` at scales `safeScale / (n + 1)` converges to the
  literal five-piece carrier. The focused support build passes and the
  declaration audit reports only `propext`, `Classical.choice`, and
  `Quot.sound`. Literal-frontier domination, the sharp weighted upper bound,
  and profilewise relaxed-perimeter equality remain pending, so no
  unconditional density coverage is added.
- Round 245 completes strategy-00050 milestone 3 under prospective
  same-invocation admission. `CMVProfileSharpRecovery` works for every
  `profile : CanonicalTypeIVProfile lam`: exact attachment cancellation gives
  all four normalized junction branches Lipschitz constants proportional to
  `epsilon`, despite profiles whose attachment width can be arbitrarily small.
  Their Euclidean `H¹` union is bounded by one finite profile-dependent
  `C * epsilon`. Literal frontier domination covers the complete actual
  canonical frontier outside the open collars and the four moving traces
  inside them, including both collar endpoints and both poles. The resulting
  smooth-cost estimate preserves the actual canonical frontier integral with
  density `StripDensity lam` and charges only a finite `O(epsilon)` excess.
  The existing universal recovery sequence consumes that estimate in
  `relaxedPerimeter`; combined with the retained universal lower bound, every
  canonical profile now has exact extended relaxed-perimeter equality. The
  focused support build passes and the declaration audit reports only
  `propext`, `Classical.choice`, and `Quot.sound`. Finite source admissibility,
  real perimeter equality, and the unchanged compatibility consumer remain
  pending, so no unconditional density coverage is added.
- Round 246 completes strategy-00050 milestone 4 under prospective
  same-invocation admission. `CMVProfileSourceSemantics` uses the exact
  profilewise relaxed-perimeter equality to prove that every canonical
  type-(iv) carrier is admissible for `relaxedSourceSemantics`: its carrier is
  null measurable, its extended relaxed perimeter is strictly below infinity,
  and its weighted area is integrable. After proving nonnegativity of the
  literal complete-frontier integral, it converts the extended equality to the
  real identity required by the unchanged `profile_perimeter_eq` field.
  `compatibleWithModel_of_model_covered` now constructs the complete unchanged
  compatibility structure from only its existing arbitrary-competitor
  `model_covered` proposition. Canonical, exactly horizontally congruent, and
  almost-everywhere horizontally congruent all-density non-minimizer consumers
  use that constructor without caller-supplied profile recovery, finiteness, or
  perimeter equality. The focused support build passes and the declaration
  audit reports only `propext`, `Classical.choice`, and `Quot.sound`. Universal
  source classification and arbitrary-competitor model coverage remain
  unresolved, so no unconditional source theorem is claimed.
- Round 247 completes strategy-00050 milestone 5 under prospective
  same-invocation admission. The independently rerun
  `CMVSquaredWidthRecoverySupport` changed root builds all 8,732 jobs; direct
  aggregate audits cover 21 arbitrary-profile recovery declarations and eight
  source-semantics/consumer declarations, each with only `propext`,
  `Classical.choice`, and `Quot.sound`. A contract smoke check confirms that
  canonical admissibility, finite-value real perimeter equality, and the exact
  and almost-everywhere all-density consumers retain only the unchanged
  arbitrary-competitor `model_covered` premise. The support target contains all
  five strategy roots, and a fail-closed scan finds no `sorry`, `admit`, project
  `axiom`, or `native_decide`. The source-locator audit checks the retained CMV
  source at Section 2 (printed pages 2--3), Proposition 3.6 and equation (21)
  (page 14), Lemma 3.8 Step 2 and equations (24)--(25) (pages 15--17),
  Proposition 3.9 (page 18), and Conjecture 3.12 with equation (27) (page 19).
  Resource reconciliation stays below every strategy cap with a 10 GiB memory
  ceiling, swap disabled, and no OOM event. Strategy `strategy-00050` is
  complete. This verifies universal canonical recovery and its conditional
  consumers, not universal source classification, arbitrary-competitor model
  coverage, or unconditional CMV resolution.
- Rounds 248--253 complete strategy-00051. For every actual
  `TypeThreeAssembly`, the checked lower-contact and upper-contact surgeries,
  together with the exact open radius-two disk at `h = 1/2`, form a
  branch-complete `SmoothSequence` converging to the literal closed carrier.
  Literal-frontier domination and density-one lower replacement traces give a
  sharp weighted-cost upper bound by the assembly's actual complete-frontier
  perimeter; hence every type-(iii) carrier is source-admissible with finite
  real source perimeter bounded by that frontier value. The universal
  stationary equal-area witness now supplies such an actual type-(iii)
  competitor with strictly smaller source perimeter than every canonical
  regular type-(iv) carrier. Canonical, exact-horizontal, and
  almost-everywhere-horizontal source non-minimizer consumers therefore require
  no `model_covered`, `CompatibleWithModel`, or caller-supplied recovery
  comparison. The final `CMVTypeThreeLowerRecoverySupport` build checks all
  8,738 jobs; aggregate audits of 60 strategy declarations report only
  `propext`, `Classical.choice`, and `Quot.sound`; the compiled consumer,
  source-locator, support-scope, forbidden-mechanism, and cumulative resource
  audits pass under the prospective 10 GiB ceiling with swap disabled and no
  OOM event. Strategy `strategy-00051` is complete. Universal source
  classification, including endpoint coverage, remains unresolved, so this is
  not yet an unconditional proof of CMV Conjecture 3.12.
- Round 254 completes strategy-00052 milestone 1. The shared upper-cap and
  right-side tangent-patch implementations now work directly on every
  `FourArcCandidate`, including `h = 1`, over the literal
  `candidate.assembly.carrier`; the right windows use the literal
  `capChord / 2` boundary. The checked endpoint specialization fixes
  `h = 1`, `alpha = arccos (1 / lambda)` for every `lambda > 1`, constructs
  both finite pairwise-disjoint families for every positive deficit, and
  identifies their complete-arc targets as
  `2 * lambda * arccos (1 / lambda)` and `pi`. Each family retains its actual
  lower/upper collar certificates, full-window density localization, finite
  summed error, and one global `smoothCost` charge. The regular canonical
  four-arc exhaustion, lower bound, and frozen/arbitrary-profile recovery
  support targets compile through the migrated shared implementation. Targeted
  source checks and the five-declaration audit pass with only `propext`,
  `Classical.choice`, and `Quot.sound`. Candidate-level reflections, combined
  four-family exhaustion, and the endpoint relaxed lower bound remain pending,
  so no endpoint source exclusion or universal classification is claimed.
- Round 255 completes strategy-00052 milestone 2. Carrier reflection now works
  directly on every literal `FourArcCandidate.assembly.carrier`, including
  `h = 1`. The reflected lower-cap and left-side families share the migrated
  upper-cap and right-side implementations. Strict upper/lower/left/right
  regions prove within- and cross-family disjointness, so one finite-summation
  application charges all four arc families to one global `smoothCost`.
  `CandidateFourArc.frontierWeightedPerimeter_eq_four_components` identifies
  their targets with the actual complete-frontier integral, and sequence-liminf
  removal proves `CandidateFourArc.frontierCost_le_relaxedPerimeter` for every
  candidate and every `lambda > 1`. The explicit endpoint accounting is
  `4 * lambda * arccos (1 / lambda) + 2 * pi`, and the literal endpoint carrier
  satisfies the same extended lower bound. Regular canonical recovery callers
  compile through thin specializations. The combined endpoint, lower-bound,
  and arbitrary-profile recovery support build checks 8,737 jobs; focused
  audits report only `propext`, `Classical.choice`, and `Quot.sound`. Endpoint
  source admissibility and the recovered type-(iii) source-minimality
  contradiction remain pending, so no universal classification is claimed.
- Round 256 completes strategy-00052 milestone 3. For every literal
  `FourArcCandidate` satisfying the source incidence law, including `h = 1`,
  the universal scalar witness is realized by an actual source-admissible
  `TypeThreeAssembly` with equal weighted area and strictly smaller literal
  complete-frontier perimeter. The type-(iii) recovery upper bound and the
  candidate four-arc projection lower bound convert this to a strict comparison
  of actual extended relaxed perimeters. If the candidate carrier were a source
  minimizer, its own admissibility would supply the only needed finiteness and
  convert the minimizer's real inequality back to the opposite extended
  inequality. Thus the literal endpoint carrier is excluded for every
  `lambda > 1` without supplied finiteness, endpoint recovery, compatibility,
  or `model_covered`. The recovered type-three support build checks all 8,738
  jobs, and the focused audit reports only `propext`, `Classical.choice`, and
  `Quot.sound`. Exact and almost-everywhere horizontal endpoint transport and
  the source-radius-one classification branch remain pending, so no universal
  classification is claimed.
- Round 257 completes strategy-00052 milestone 4. A shared closed-Snell
  raw-coordinate interface now covers every `sourceRadius >= 1`, identifies
  its literal five-piece carrier with one `FourArcCandidate`, proves exact
  agreement with the existing canonical normalization when `sourceRadius > 1`,
  and proves exact agreement with the explicit endpoint when
  `sourceRadius = 1`. Candidate non-minimality transports directly through
  exact and almost-everywhere horizontal representatives at the level of
  weighted area and extended relaxed perimeter; it does not require endpoint
  recovery, source finiteness of the normalized carrier, compatibility, or
  `model_covered`. The endpoint, raw closed-Snell, strict regular, and
  radius-one source-facing exclusions all kernel-check. The recovered
  type-three and source-classification support builds check 8,739 and 8,704
  jobs respectively, and the focused audit reports only `propext`,
  `Classical.choice`, and `Quot.sound`. Universal geometric/GMT classification
  into the raw-coordinate interface remains unresolved, so no unconditional
  source theorem is claimed.
- Round 258 completes strategy-00052 milestone 5. One registered changed-root
  build covers the endpoint patch gate, candidate four-arc exhaustion,
  candidate/canonical relaxed lower bound, arbitrary-profile recovery,
  branch-complete type-(iii) recovery and source exclusion, and source
  classification support; all 8,753 jobs pass. The aggregate audit checks 43
  strategy declarations and reports only `propext`, `Classical.choice`, and
  `Quot.sound`. A compiled contract exercises literal-candidate, endpoint,
  exact-horizontal, almost-everywhere-horizontal, closed-Snell, and
  radius-one consumers without supplied finiteness, recovery, compatibility,
  or `model_covered`. The support registration, shared-implementation cutover,
  and forbidden-mechanism scans pass. A renewed source audit records
  Proposition 3.6 and equation (21) (printed page 14), Lemma 3.8 and Steps
  1--3 with equations (24)--(25) (pages 15--18), Proposition 3.9 (page 18),
  and Conjecture 3.12 with equation (27) (page 19), while explicitly keeping
  Figure 5 distinct from the literal four-arc endpoint. Prospective
  same-invocation containment remained at 10 GiB with swap disabled and no
  memory event. Strategy `strategy-00052` is complete. Universal source
  classification remains unresolved, so no unconditional CMV theorem is
  claimed.

- Round 259 completes strategy-00053 milestones 1--2. An independent
  source-incidence signature records both vertical orientations of CMV
  Lemma 3.8 Figure 3 using actual circle caps, centers, endpoints, signed
  Snell data, and two closed interface segments whose lengths may vanish. It
  contains no `TypeThreeAssembly`, normalization, available-width formula,
  chord comparison, or non-fit conclusion, and it explicitly retains the
  `sourceRadius = 1` Figure-5 boundary. `CMVFigureThreeNonfit` identifies the
  actual lower-interface section of every translated `TypeThreeAssembly`,
  derives the strict exterior-chord inequality from equal radius and
  `lambda * cos theta = 1`, and proves that the literal chord cannot be
  contained in that actual section for any horizontal placement. A separate
  specialization proves that the `h = 1/2`, radius-two section is exactly a
  singleton and satisfies the same non-fit result. The registered 8,706-job
  support build and compiled signature contract pass; audits of all six
  declarations report only `propext`, `Classical.choice`, and `Quot.sound`.
  Deriving a `TypeThreeAssembly` and chord containment from the independent
  source incidence remains pending, so no Figure-3 source-branch elimination
  or universal classification is claimed.

- Round 260 completes strategy-00053 milestone 3. From every strict-radius
  independent `CMVFigureThree.SourceIncidence`, `CMVFigureThreeNormalization`
  constructs the branch-complete `TypeThreeAssembly` with curvature
  `1 / sourceRadius`, proves its radius is the source radius, and derives the
  primary exterior angle from the signed Snell equation over the full
  `(0, pi)` range. The source circle equations and outward-side inequalities
  select both tangency abscissae as
  `symmetryAxisX +/- assembly.sideHalfWidth`. After the orientation-dependent
  vertical reflection, the literal closed interval between the actual source
  tangencies is exactly the `y = -1` section of the horizontally translated
  assembly carrier. The theorem covers both orientations and the radius-two
  semicircular transition without fixing horizontal placement. The registered
  8,708-job support build and compiled contract pass; audits of all eleven new
  public declarations report only `propext`, `Classical.choice`, and
  `Quot.sound`. Added-cap chord containment and the strict-radius incidence
  contradiction remain pending, so no Figure-3 source-branch elimination or
  universal classification is claimed.

- Round 261 completes strategy-00055 milestone 1 and advances milestone 2.
  The two non-strict interface-segment orders now kernel-derive literal
  containment of the actual added-cap chord between the source tangencies,
  including either degenerate segment. Orientation normalization transports
  that same cap's complete arc and chord to a lower cap while preserving its
  radius, angle, horizontal placement, and contact law. Composing this
  containment with the source-derived tangent-section normalization and the
  retained literal non-fit theorem proves that every strict-radius
  `CMVFigureThree.SourceIncidence` is contradictory and leaves the exact
  `sourceRadius = 1` residual. The independent
  `ActualBoundaryRealization` signature starts with an actual planar carrier
  whose complete frontier is the literal union of both exterior circles, both
  strip-side circles, and both possibly degenerate interface segments; actual
  trace membership and endpoint order derive every `SourceIncidence` field.
  Its strict-radius consumer is contradictory and its closed-radius consumer
  likewise forces radius one. Both vertical orientations, the full primary
  angle range, arbitrary horizontal placement, and the radius-two transition
  remain covered. The registered 8,711-job support build and compiled contract
  pass; audits of all new declarations report only `propext`,
  `Classical.choice`, and `Quot.sound`. Extracting this boundary realization
  from every relevant regular source minimizer, treating the separate
  radius-one Figure-5 branch, and universal source classification remain
  unresolved.

- Round 262 completes strategy-00055 milestone 2. The source-located
  `RegularFigureThreeBoundaryConfiguration` ties an actual carrier to source
  minimality while keeping regular Snell data, vertical reflection symmetry,
  equal-curvature circular continuation, and exhaustive Figure-3
  configuration enumeration as four separate upstream inputs. From those
  inputs, `CMVFigureThreeSourceExtraction` derives both strip-side join
  memberships, both non-strict endpoint orders (including degenerate
  segments), and the exact six-piece topological-frontier equation, then
  constructs the existing `ActualBoundaryRealization`. Its strict-radius
  consumer is contradictory and its closed-radius consumer retains exactly
  `sourceRadius = 1` without identifying that residual with the literal
  four-arc endpoint or discarding it as Figure 5. The registered 8,713-job
  support build and compiled source contract pass; all thirteen audited
  declarations report only `propext`, `Classical.choice`, and `Quot.sound`.
  Deriving the four separated configuration inputs from every relevant regular
  source minimizer and eliminating the distinct radius-one Figure-5 branch
  remain unresolved, so no universal classification is claimed.

- Round 263 advances strategy-00055 milestone 3. The source-located
  `PiecewiseRegularBoundaryTrace` now records one actual closed boundary curve,
  six ordered parameter intervals, `C¹` regularity with nonzero interior
  velocity on each nondegenerate piece, exact images of the two exterior arcs,
  two strip-side arcs, and two possibly degenerate interface segments, and the
  complete actual-frontier image. The six-interval partition kernel-derives
  `ConfigurationEnumerationData`'s pointwise exhaustive cover and all six
  actual-component inclusions. `RegularFigureThreeBoundaryConfiguration` no
  longer assumes that enumeration; it separately accepts interface endpoint
  incidence and the piecewise trace, then derives the retained actual-boundary
  realization, strict-radius contradiction, and exact radius-one residual.
  The registered 8,713-job support build and compiled source contract pass;
  all twenty-three audited declarations report only `propext`,
  `Classical.choice`, and `Quot.sound`. Deriving this source-faithful trace and
  the remaining local Snell, symmetry, continuation, and endpoint data from
  every relevant regular minimizer, and eliminating the distinct radius-one
  Figure-5 branch, remain unresolved.

- Round 264 completes strategy-00055 milestones 3--4. A
  source-level `ClosedBoundaryTrace` now records the one actual closed curve,
  its ordered global parameter interval, and its complete-frontier image before
  Lemma 3.8 case selection. The subsequent `FigureThreeBranchSelection`
  supplies five cuts, six piecewise-regularity certificates, and six pointwise
  trace characterizations; these kernel-derive all six exact component images.
  Its two non-strict endpoint orders construct the possibly degenerate interface
  segments and derive their full endpoint-incidence record definitionally.
  `RegularFigureThreeBoundaryConfiguration` therefore no longer accepts
  interface segments, `InterfaceIncidenceData`, or
  `PiecewiseRegularBoundaryTrace` as primitive fields. The derived
  actual-boundary realization, normalization/containment contradiction,
  strict-radius exclusion, and exact radius-one residual remain intact. One
  integrated 8,752-job build covers Figure-3 extraction, source classification,
  and the branch-complete type-(iii) direct exclusions; two compiled contracts
  pass, and all twenty-nine audited source-extraction declarations report only
  `propext`, `Classical.choice`, and `Quot.sound`. Deriving the closed curve,
  Figure-3 branch selection, regular Snell data, vertical symmetry, and circular
  continuation from every relevant regular source minimizer, and eliminating
  the distinct radius-one Figure-5 branch, remain unresolved.


- Round 265 produces strategy-00056 milestone-1 mathematics and advances its joint
  milestone-2 gate. `CMVFigureFourSourceGeometry` freezes an actual source set
  and bounded open almost-everywhere representative, arbitrary generic interval
  sections, four literal common-radius circle traces with actual junctions and
  branch data, vertical-line reflection, signed Snell laws, complete frontier,
  and explicit local one-sided circle equations away from junctions. It does
  not contain raw coordinates, scalar height or radical alignment, horizontal
  symmetry, a principal angle, normalized widths, target sections, carrier
  identification, or comparison data. A proved finite exceptional-height set
  removes only interfaces and circle tangencies. From these primitives,
  `CMVFigureFourScalarReduction` derives every premise of
  `figure4_scalar_reduction`, horizontal center symmetry, `R ≥ 1`, the
  principal closed-Snell angle, and actual generic-section endpoints from
  transverse intersections on both strip-side circles and both exterior caps.
  The transverse bridge uses the representative's local one-sidedness to
  construct horizontal inside/outside sequences; no section-boundary equality
  is assumed. Explicit `lambda = 2`, `R = 2` and `R = 1` raw closed-Snell
  witnesses exercise both resulting parameter branches. The registered
  8,707-job support build passes; all twenty-four audited declarations report
  only `propext`, `Classical.choice`, and `Quot.sound`. Full source-geometry
  instances for those two radii, actual-to-raw almost-everywhere reconstruction,
  and extraction of the frozen primitives from arbitrary minimizers remain
  unresolved; therefore the A+3 non-vacuity gate is not complete. Final resource
  reconciliation measured 324.493368 CPU seconds, 2,146.817666 wall seconds,
  and an 8,836,349,952-byte memory peak under the admitted 10-GiB/no-swap
  cgroup. The 1,800-second invocation wall cap was exceeded. Under the
  strategy's mandatory kill criterion, strategy-00056 is terminated despite
  the compiling retained mathematics; do not continue it or automatically
  switch to the below-policy Figure-5 reserve.

- Round 266 completes strategy-00057 milestone 1. The independent
  `CMVFigureFive.SourceGeometry` signature freezes an actual source carrier,
  bounded open almost-everywhere representative, two common-radius strip
  circles, one or two independently placed exterior caps, every possibly
  degenerate exposed interface segment, the literal complete frontier, and
  local one-sided circle data. It contains no normalized carrier, target area,
  perimeter barrier, recovery, comparison, or source-minimality field.
  The same round seeds `CMVFigureFiveAngular` with the positive-branch source
  identity for `h = (1 + cos u) / 2` and the exact derivative
  `angularArea' 0 = -4`; no negative-angle source identity is asserted.
  The registered source-signature build checks 8,710 jobs and all four audited
  source declarations report only `propext`, `Classical.choice`, and
  `Quot.sound`. Radius-one reconstruction and the two compulsory source
  specimens remain pending.

- Round 267 completes strategy-00057 milestone 2. The shared type-(iii)
  support argument now includes the closed endpoint by proving actual
  `typeThreeArea` and area-support continuity at `h = 1` while using fold
  derivatives only in `0 < h < 1`. The negative angular derivative supplies a
  regular anchor below the endpoint area. Area coercivity at zero, the
  generalized endpoint-maximum theorem, and one strictly positive support
  slope then prove that every
  `V >= typeThreeArea lambda 1` is attained by some `0 < r < 1` with
  `typeThreePerimeter lambda r <
  V + pi + endpointGap lambda`. The existing branch-complete
  `TypeThreeAssembly` recovery realizes this as an actual source-admissible
  carrier of weighted area `V`; in particular, a compiled contract exercises
  the threshold equality without constructing an assembly at `h = 1`.
  The registered 8,741-job support build passes and all fourteen audited
  declarations report only `propext`, `Classical.choice`, and `Quot.sound`.
  The full non-vacuous threshold and off-center Figure-5 source instances,
  segmented relaxed lower bounds, and source-connected strict comparisons
  remain pending, so no Figure-5 source exclusion or universal classification
  is claimed.

- Round 270 completes strategy-00060 milestones 1--2 ahead of the A+2
  deadline. `CMVSourceSectionClassification` kernel-checks that horizontal
  sections preserve bounded openness, that every section-frontier point lifts
  to the planar frontier, and that a bounded open real set with complete
  frontier `{a,b}` for `a < b` is exactly `Ioo a b`.
  `CMVFigureFour.SourceRepresentative` now contains only bounded openness and
  almost-everywhere source agreement; the supplied `generic_interval_sections`
  field is removed. For every Figure-4 `SourceGeometry`, every
  nonexceptional `|y| < 1` section is derived directly as the open interval
  between the two actual strip-circle intersections by combining the complete
  four-circle frontier with transverse local one-sidedness. The proof uses
  only the already-derived `sourceRadius >= 1`, so it retains the radius-one
  branch. An independently defined Euclidean open unit disk exercises the same
  finite-crossing reconstruction without a section formula premise. The
  registered Figure-4 and migrated Figure-5 support builds check 8,707 and
  8,741 jobs; all new declarations report only `propext`,
  `Classical.choice`, and `Quot.sound`. The complete Figure-4 source specimens,
  exterior-height section regimes, almost-everywhere raw-carrier
  reconstruction, and source exclusion remain pending.

- Round 273 terminates strategy-00062 at its first milestone. The sole bounded
  assignment derived reusable bounded-open outward-ray topology and a
  denominator-free proof that every positive-contact Figure-5 cap trace stays
  horizontally between its chord endpoints. A direct source check passed on
  that core before later unverified source-half-plane edits; those edits were
  rolled back, and the retained `CMVFigureFiveSegments` checkpoint is
  unregistered and has no allowed-axiom audit. The unchanged-signature capped
  segment half-planes, actual flat projection patches, and complete off-center
  open-union `SourceGeometry 2` specimen remain absent. New proof editing
  continued after the strategy's explicit 900-second invocation boundary
  (first observed late edit at 1013.58 seconds), so the resource kill criterion
  is triggered despite no OOM event under the 10-GiB/no-swap cgroup. Do not
  restart or rename this campaign. Exact hashes, missing obligations, and
  resource observations are retained in round 273's
  `strategy-00062-milestone-1-failure.json`; no relaxed lower bound, source
  exclusion, or density coverage was added.

- Round 274 completes strategy-00063 milestone 1 ahead of its A+2 deadline.
  `CMVTypeThreePhaseThreshold` defines the actual type-(iii) support gap
  `S(h) = typeThreePerimeter lambda h - typeThreeArea lambda h - pi` and
  kernel-checks `S'(h) = (h - 1) * typeThreeFold lambda h / h^3` on
  `0 < h < 1`, the exact half-height value `S(1/2) = -pi`, continuity and the
  positive endpoint value `S(1) = endpointGap lambda`, and the universal
  barrier `S(h) <= 0 -> typeThreeFold lambda h < 0`. The endpoint argument
  uses the actual finite area and perimeter formulas and never evaluates the
  singular fold at `h = 1`. The same module proves
  `pi < typeThreeArea lambda h` for every `1/2 <= h < 1` by exact arccos and
  scaled-radical comparisons. The registered support build checks all 8,737
  jobs; all seven declarations report only `propext`, `Classical.choice`, and
  `Quot.sound`, and the forbidden-mechanism scan is empty. This establishes
  the first analytic gate only: the unique support-gap crossing, complete sign
  partition, phase threshold, all-volume classification, and source-admissible
  witnesses remain pending.

- Round 275 completes strategy-00063 milestone 2 ahead of its A+4 deadline.
  `CMVTypeThreePhaseThreshold` now kernel-checks existence and uniqueness of
  the actual support-gap crossing in `1/2 < h < 1`. Reusing the existing
  strictly increasing type-(iii) fold and the nonpositive-gap fold barrier, it
  proves the complete strict sign partition: the support gap is negative
  below the crossing, zero exactly there, and positive above it throughout
  `0 < h < 1`. The internally defined phase threshold is the actual
  `typeThreeArea` at that crossing and is strictly above `pi`. No inverse,
  duplicate branch machinery, or endpoint fold evaluation is used. The
  registered 8,737-job support build passes; all fourteen audited declarations
  report only `propext`, `Classical.choice`, and `Quot.sound`, and the
  forbidden-mechanism scan is empty. All-volume area equivalences, complete
  competing-root classification, and source-admissible witnesses remain
  pending.

- Round 276 advances strategy-00064 milestone 1. A shared
  `finiteMesh_index_bounds_and_sin` theorem replaces the four-arc upper-cap
  separation proof's minor-angle restriction by the exact full-range
  finite-index bounds `d <= x <= theta - d` for
  `0 < theta < alpha < pi`; its split proof reflects the branch above
  `pi/2` through `sin (pi - x) = sin x` and introduces no
  `d < pi - theta` guard. The existing four-arc upper-cap exhaustion compiles
  through that theorem. `TypeThreeAssembly` now also identifies the literal
  source carrier with its actual exterior cap pointwise and as a set above
  `y = 1`. The registered 8,708-job upper-cap support build passes, and the
  four audited declarations report only `propext`, `Classical.choice`, and
  `Quot.sound`. The shared generic patch construction, universal
  arbitrary-deficit type-(iii) upper-cap family, and compiled
  `lambda = 2`, `h = 1/4` major-arc application remain pending, so the A+3
  gate is advanced but not complete.

- Round 277 produces strategy-00064's complete milestone-1 mathematics but
  falsifies its acceptance contract. `UpperCapPatchSource` isolates the literal
  carrier/cap applicability data, and one full-angle `UpperCapPatch`
  implementation constructs the tangent frame, collars, exterior-density
  certificate, finite-mesh separation, pairwise-disjoint windows, finite error,
  arbitrary-deficit payoff, and one-charge smooth-cost bound. Both
  `FourArcCandidate.assembly.carrier` and every actual branch-complete
  `TypeThreeAssembly.carrier` instantiate it. The compiled `lambda = 2`,
  `h = 1/4` source assembly proves `pi/2 < outerAngle` and exercises the
  universal major-cap theorem. The 8,708-job support build and migrated
  8,710-job four-arc build pass; audits report only `propext`,
  `Classical.choice`, and `Quot.sound`. Final verification continued after the
  strategy's explicit 660-second checking boundary, so its resource kill
  criterion is triggered and strategy-00064 is terminated despite the retained
  kernel-checked declarations. Do not continue or rename this campaign. No
  type-(iii) relaxed lower bound or equality is claimed.

- Round 292 completes strategy-00076 milestone 1. `CMVFiniteJunctionDisagreement`
  defines open local disagreement phases on the plane punctured only at the
  junction set, while testing membership labels away from a separate null set.
  Null-complement density proves that the agreement and disagreement phases
  cannot overlap. Fixed-orientation refinements of the frozen
  `CMVFigureFour.LocallyOneSided` interface prove both the matching-circle-side
  zero label and opposite-circle-side one label using the literal
  `circleValue = 0` exceptional set; no global orientation agreement, source
  section, or source-to-target equality is assumed. The registered 8,708-job
  Figure-4 support build passes, and the focused audit reports only `propext`,
  `Classical.choice`, and `Quot.sound`. The full finite-junction connectivity,
  exterior-anchor, and almost-everywhere rigidity theorem remains pending, so
  no Figure-4 reconstruction or source exclusion is claimed.

- Round 293 completes strategy-00076 milestone 2.
  `CMVFiniteJunctionDisagreement` kernel-checks that the complement of every
  finite subset of `PlanePoint` is connected by the explicit real-rank-two
  calculation. For arbitrary bounded planar sets, finiteness also constructs
  an actual common exterior neighborhood carrying the agreement phase.
  Therefore, if the two audited open local phase sets cover the punctured
  plane, null-complement label uniqueness and connectedness eliminate the
  disagreement phase and prove almost-everywhere equality after removing only
  the separate null test set and finite junction set. The registered
  8,708-job Figure-4 support build passes, and the focused audit reports only
  `propext`, `Classical.choice`, and `Quot.sound`; the forbidden-mechanism scan
  is empty. Deriving phase coverage and local target orientations from the
  literal closed-Snell target geometry remains pending, so no Figure-4
  reconstruction or source exclusion is claimed.

- Universal source classification remains unresolved; therefore the checked
  exact- and almost-everywhere-horizontal direct consumers do not yet yield an
  unconditional source theorem. Arbitrary-competitor `model_covered` and
  general reduced-boundary/model compatibility also remain unresolved, but
  neither is a premise of the direct recovered type-(iii) comparison.

Do not interpret a completed campaign, retained report, successful process exit,
source hash, or generated Boolean as a proof.

## Current critical path

1. Preserve the proved density prefix, cutoff, atlas replay modules, numerical
   certificates, reusable source-native scalar normalization/interval/cell
   modules, and the checked `CMVRelaxation` transport, representative
   invariance, finite source-semantics adapter, normalization constructors, and
   normalization-free cutoff consumers. Do not represent the generated
   strategy-00040 stress modules as proofs, and do not restart that terminated
   method under another name. Do not resume serial seam-cell replay,
   cusp-majorant sharpening, or denominator shaving.
2. Preserve strategy-00044's completed frozen `lambda = 2`, `h = 1/2` A+4
   recovery gate: one global squared-width `IsSmoothDomain` family for every
   `0 < epsilon <= 1/16`, global characteristic convergence, explicit
   junction-value cancellation, and a weighted-cost bound with one finite
   epsilon-independent constant.
3. Preserve the checked rigid-coordinate transport, localized weighted defect,
   pairwise-disjoint finite summation primitive, arbitrary-profile four-arc
   patch exhaustion, cross-family disjointness, exact complete-frontier payoff
   identification, and the universal lower bound obtained by removing every
   fixed patch deficit and finite characteristic-distance error along actual
   convergent `SmoothSequence` values.
4. Preserve the arbitrary-profile squared-width recovery, literal-frontier
   domination, exact extended relaxed-perimeter equality, canonical relaxed
   source admissibility, and finite-value real perimeter equality. Also
   preserve the branch-complete type-(iii) smooth recovery, sharp weighted
   frontier upper bound, source admissibility, and canonical/exact-horizontal/
   almost-everywhere-horizontal direct exclusion with no `model_covered` or
   `CompatibleWithModel` premise.
5. Preserve the candidate-level reflection, one-charge four-family exhaustion,
   exact complete-frontier accounting, extended relaxed lower bound, recovered
   type-(iii) strict extended comparison, and source-minimality contradiction
   for every `FourArcCandidate`, including `h = 1`. Preserve exact/AE
   horizontal endpoint transport and the shared `sourceRadius >= 1`
   raw-coordinate carrier identification whose strict and radius-one branches
   agree with the canonical normalization and explicit endpoint.
6. Prove the remaining universal source classification, including the excluded
   Lemma 3.8 configurations, into the checked exact- or
   almost-everywhere-horizontal interface. For Figure 3, preserve the checked
   extraction from separately stated regular Snell, vertical-symmetry,
   circular-continuation, one pre-selection closed source boundary curve, and
   its Figure-3 branch selection into `ActualBoundaryRealization`; derive these
   inputs from every relevant source minimizer. Its checked consumer eliminates
   every strict-radius branch
   while retaining the explicit radius-one Figure-5 residual. Then eliminate
   the remaining Figure-4 and Figure-5 configurations and compose universal
   classification with the direct all-density source exclusion into CMV
   Conjecture 3.12. Keep
   arbitrary-competitor model coverage explicit as an unresolved auxiliary
   route rather than weakening it. Run the full build, allowed-axiom audit,
   source-locator audit, and deterministic certificate reproduction before
   claiming completion.

The round-192 numerical pilot and the explicit near-one cell prefix remain
independently retained evidence, but they are no longer needed for modeled
all-density exclusion. Do not regenerate the finished `lambda >= 51/50`
inventory unless a concrete audit failure invalidates it.


## Proof and evidence requirements

- Never use `sorry`, `admit`, project axioms, `native_decide`, or an unchecked
  numerical oracle.
- Formal claims require exact Lean declarations, a successful targeted build,
  and an allowed-axiom audit.
- Numerical claims require retained inputs, precision, algorithms, commands,
  outputs, and an independently checked soundness path.
- Source claims require precise page, theorem, proposition, equation, or line
  locators.
- Never weaken the requested theorem to make a campaign appear successful.
- Distinguish proved, interval-certified, numerically observed, conditional,
  inferred, conjectured, rejected, and unresolved statements.
- Preserve useful partial work in small compiling modules before long searches
  or builds.

## Autorun strategy reflection

Every 150 minutes, pause task selection and reassess:

1. Is the current approach still the shortest credible route to the complete CMV
   result?
2. Did new evidence invalidate an assumption, interface, or certificate strategy?
3. Are agents repeating a failed approach or optimizing a non-critical component?
4. Would a specific change to Agentic Lean Math Assistant improve reliability,
   proof throughput, failure recovery, or audit correctness?
5. What is the single highest-value verified action for the next work period?

A reflection is not a stopping point. Record the conclusion, update the working
strategy when evidence requires it, and immediately execute the selected action.

## Anti-stall policy

- Never wait for the user during autorun.
- If one path is blocked, retain the exact blocker and switch to another
  independent critical-path obligation.
- Never retry an unchanged failed command or proof idea.
- Treat timeouts and empty agent output as recoverable failures: inspect retained
  partial work, choose a smaller task, and continue.
- Prefer targeted Lean builds and split oversized generated modules.
- Do not rerun the monolithic `NearOneThirdFrozenEstimate.lean` check unchanged:
  it exhausted a 12 GiB cgroup in round 147. First isolate the smallest import
  root or dependency needed to validate the retained cancellation checkpoint.
- Do not poll a process manually; use bounded commands and durable receipts.
- Keep every command inside the inherited resource-control cgroup. Never use
  `systemd-run`, `nohup`, `disown`, `setsid`, or background jobs; retain durable
  evidence from bounded foreground commands instead.
- The controller may stop only on an explicit stop request or after the complete
  success contract has been independently verified.

## User-editable contract

This file is the living Master Prompt. The user may edit it while autorun is
active. Each new autorun round must reload it; subsequent work follows the newest
saved contents without rewriting the user's objective.
