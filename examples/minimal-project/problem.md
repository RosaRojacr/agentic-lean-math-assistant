# Minimal Example Problem

Using the retained source, prove that the area of a real square with side length \(s\) is nonnegative. State the domain, derive the formula, and distinguish the zero and positive cases. A complete result must be concise, source-linked, and independently checked.
